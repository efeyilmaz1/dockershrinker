apiVersion: v1
kind: Service
metadata:
  name: {{ include "smartdockershrinker.fullname" . }}
  labels:
    {{- include "smartdockershrinker.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "smartdockershrinker.selectorLabels" . | nindent 4 }}
